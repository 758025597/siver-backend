# config

Clases de configuracion anotadas con `@Configuration`.

Lo primero que va a hacer falta: CORS. Sin eso el frontend Angular
(siver-frontend, en otro puerto) no va a poder consumir la API y el
navegador bloquea las peticiones.
